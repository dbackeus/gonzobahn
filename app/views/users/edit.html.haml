-@page_title = title_for(@user, "edit")

- form_for @user do |f|
  = f.error_messages
  
  - unless @user.open_id?
    = f.password_field :password
    = f.password_field :password_confirmation
  
  = f.submit t("save")