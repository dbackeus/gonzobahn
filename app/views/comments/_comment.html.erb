<% div_for comment do %>
  <% if comment.user == current_user %>
  <% end %>
  <cite><%=time_ago comment.created_at %> <%= link_to comment.user, user_recordings_path(comment.user) %> said </cite>
  <%= RedCloth.new(comment.comment).to_html %>
<% end %>